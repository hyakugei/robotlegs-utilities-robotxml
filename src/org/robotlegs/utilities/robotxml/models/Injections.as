package org.robotlegs.utilities.robotxml.models {		import org.robotlegs.mvcs.Actor;		public class Injections extends Actor	{				private var injectionPoints:Array = new Array();				public function Injections() 		{		}				public function addInjections(fqdn:String, names:Array):void		{			//trace(fqdn + " " + names);			injectionPoints.push({fqdn:fqdn, names:names});		}				public function getInjections():Array		{			return injectionPoints.concat();		}	}}